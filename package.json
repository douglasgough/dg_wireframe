{
  "name": "dg-wireframe",
  "version": "1.0.0",
  "type": "module",
  "description": "Static website",
  "scripts": {
    "build:css": "postcss app.css -o dist/app.css --verbose",
    "watch:css": "postcss app.css -o dist/app.css --watch --verbose"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.0.0"
  }
}
