{
  "name": "dg-wireframe",
  "version": "1.0.0",
  "description": "Static website with live reload",
  "scripts": {
    "start": "live-server --port=8080 --watch=. --open=index.html",
    "dev": "live-server --port=8080 --watch=. --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}